# Эмиль Арифулин

Студент 3 курса ФПМИ МФТИ, направление «прикладная математика и информатика». Интересуюсь бэкендом и системным программированием.

## Навыки

- **Go** - горутины и примитивы синхронизации, HTTP-сервисы и middleware, `reflect`, дженерики, тестирование.
- **C++** - C++20, ручное управление памятью, аллокаторы, шаблоны, безопасность исключений.
- **Python** - Django, Flask; pandas, PyTorch, matplotlib.
- **Базы данных** - PostgreSQL, SQL.
- **Инструменты** - Linux, bash, Git, Docker, CI.
- **Математика** - теория вероятностей, математическая статистика, линейная алгебра.

## Проекты

**Курс по Go, 11 блоков заданий** (репозиторий приватный по правилам курса). Конкурентность: ограничитель частоты запросов, pub/sub, батчер, пул именованных блокировок, дедупликация одновременных вызовов, собственные реализации `once`, `rwmutex`, `cond`, `waitgroup`. HTTP: сокращатель ссылок, набор middleware, фильтрация запросов. Рефлексия и дженерики: JSON-RPC поверх `reflect`, разбор структурных тегов, обобщённые контейнеры. Прочее: `gitfame` - утилита подсчёта авторства строк по `git blame`, шифр Blowfish, поиск утечек файловых дескрипторов в тестах.

Реализации структур данных стандартной библиотеки C++ с нуля - ручное управление памятью, свои итераторы и аллокаторы, гарантии безопасности исключений:

- [deque](https://github.com/outer00/deque) - двусторонняя очередь на сегментированной памяти, итераторы произвольного доступа со сдвигом за O(1).
- [list-stackallocator](https://github.com/outer00/list-stackallocator) - двусвязный список, совместимый со стандартными аллокаторами, и bump-аллокатор поверх статического буфера: на тестовом сценарии в 4 раза быстрее `std::allocator`.
- [circular_buffer](https://github.com/outer00/circular_buffer) - кольцевой буфер, выбирающий стратегию выделения памяти на этапе компиляции.
- [shared_ptr](https://github.com/outer00/shared_ptr) - `SharedPtr` и `WeakPtr` с раздельными контрольными блоками, пользовательскими аллокаторами и делетерами.

Алгоритмы:

- [algorithms-mipt](https://github.com/outer00/algorithms-mipt) - 58 задач курса алгоритмов с условиями и разборами решений: динамическое программирование, графы, деревья поиска и отрезков, потоки и паросочетания.

## Опыт

**ЗФТШ МФТИ, преподаватель информатики** (сентябрь 2025 - май 2026). Вёл информатику у 80 учеников заочной школы: проверка работ, разбор решений, обратная связь по коду.

## Контакты

- Telegram: [@outerop](https://t.me/outerop)
